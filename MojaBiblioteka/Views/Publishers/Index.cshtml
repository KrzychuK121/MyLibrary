@model IEnumerable<MojaBiblioteka.Models.Entities.Book.Publisher>

@{
    ViewData["Title"] = "Spis wydawców";
}

<h1>Spis</h1>

<p>
    <a asp-action="Create">Dodaj wydawcę</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PublisherId">Edytuj</a> |
                <a asp-action="Details" asp-route-id="@item.PublisherId">Opis</a> |
                <a asp-action="Delete" asp-route-id="@item.PublisherId">Usuń</a>
            </td>
        </tr>
}
    </tbody>
</table>
